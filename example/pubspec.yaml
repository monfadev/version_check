name: example
description: A new Flutter project.
publish_to: 'none'
version: 2.0.7+70

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  version_check:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
